---
version: 1.0.0
events:
  build:
    steps:
      - behaviors:
          type: script
          script:
            - set -ex
            - echo "Hello Kevin Hankens from master. Update 2."
            - echo "Hello Kevin Hankens from Prs."
