---
version: 1.0.0
events:
  build:
    steps:
      - behaviors:
          type: script
          script:
            - env
            - set -ex
            - cat file.master.txt
            - cat file.pr.txt
       
