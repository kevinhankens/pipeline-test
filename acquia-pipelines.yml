---
version: 1.0.0
events:
  build:
    steps:
      - behaviors:
          type: script
          script:
            - env
            - echo "hello pr"
            - set -ex
            - cat file.master.txt
            - cat file.pr.txt
       
