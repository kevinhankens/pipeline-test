version: 1.0.0
events:
  build:
    steps:
      - 
        build_event:
          type: script
          script:
          - env
          - 'echo "GitHub build event."'
  pr-merged:
    steps:
      -
        pr_merged_event:
          type: script
          script:
            - env
            - 'echo "GitHub pr merged event."'
  pr-closed:
    steps:
      -
        pr_closed_event:
          type: script
          script:
            - env
            - 'echo "GitHub pr closed event."'
